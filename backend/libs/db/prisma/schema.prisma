generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model TelegramChannel {
  tag String @id

  @@map("telegram_channel")
}

model ChannelScrape {
  id String @id

  tag          String
  tgId         String    @map("tg_id")
  messageCount Int?      @map("message_count")
  createdAt    DateTime  @default(now()) @map("scraped_at")
  endedAt      DateTime? @map("ended_at")

  lastMessageId String? @map("last_message_id")

  @@index([tag])
  @@index([createdAt])
  @@map("telegram_channel_scrape")
}
